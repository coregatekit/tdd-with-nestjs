export enum Messages {
  USER_NOT_FOUND = 'User not found',
  USER_ALREADY_EXISTS = 'User already exists',
  CONTACT_SERIVCE = 'An error has occured please contact service',
}

export enum PrismaErrorCode {
  UNIQUE_CONSTRAINT = 'P2002',
}
